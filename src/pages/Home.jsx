import React from "react";
import Loader from "../components/loader/loader";

const Home = () => {
  return (
    <div className="home">
      <Loader />
    </div>
  );
};

export default Home;
